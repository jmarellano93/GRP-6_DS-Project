
> # Assignment 2 MLP (Multi-Layer Perceptron)
> # Supports Variable Depth (2 or 3 Layers) and Robust Scaling
> 
> library(keras3)

> library(caret)

> library(dplyr)

> library(tfruns)

> # 1. Hyperparameter flags ------------------------------------------------
> FLAGS <- flags(
+   # Regularization
+   flag_numeric("dropout1", 0),
+ .... [TRUNCATED] 

> # 2. Load & Prepare Data -------------------------------------------------
> data <- read.csv("/Users/Jujou/Documents/Repos/GRP-6_DS-Project/DS-Proj ..." ... [TRUNCATED] 

> target_col <- "target_class" 

> x_all <- data %>% select(-all_of(target_col)) %>% as.matrix()

> y_all <- to_categorical(data[[target_col]], num_classes = 8)

> # 3. Split Data ----------------------------------------------------------
> set.seed(42)

> # Holdout Test Set (15%)
> n_rows <- nrow(x_all)

> test_idx <- sample(1:n_rows, size = floor(0.15 * n_rows))

> x_test_holdout_raw <- x_all[test_idx, ]

> y_test_holdout     <- y_all[test_idx, ]

> x_cv_raw <- x_all[-test_idx, ] 

> y_cv     <- y_all[-test_idx, ]

> # 4. Cross-Validation Setup ----------------------------------------------
> k <- 5

> folds <- cut(seq(1, nrow(x_cv_raw)), breaks = k, labels = FALSE)

> # Initialize Storage
> results <- data.frame(
+   Fold = integer(),
+   Train_Loss = numeric(),
+   Val_Loss = numeric(), # Check loss function
+    .... [TRUNCATED] 

> cat("Starting", k, "-Fold Cross-Validation (Variable Depth)...\n")
Starting 5 -Fold Cross-Validation (Variable Depth)...

> # 5. Training Loop -------------------------------------------------------
> for(i in 1:k){
+   cat("\n--- Processing Fold #", i, "---\n")
+   
+    .... [TRUNCATED] 

--- Processing Fold # 1 ---
Fold 1 | Max Val Acc: 0.8504 (at Epoch 92)

--- Processing Fold # 2 ---
Fold 2 | Max Val Acc: 0.8494 (at Epoch 64)

--- Processing Fold # 3 ---
Fold 3 | Max Val Acc: 0.8435 (at Epoch 83)

--- Processing Fold # 4 ---
Fold 4 | Max Val Acc: 0.8528 (at Epoch 61)

--- Processing Fold # 5 ---
Fold 5 | Max Val Acc: 0.8467 (at Epoch 67)

> # 6. Final Report --------------------------------------------------------
> cat("\n========================================\n")

========================================

> cat("      ARCHITECTURE PERFORMANCE          \n")
      ARCHITECTURE PERFORMANCE          

> cat("========================================\n")
========================================

> avg_train_acc <- mean(results$Train_Acc)

> avg_val_acc   <- mean(results$Val_Acc)

> gap <- avg_train_acc - avg_val_acc

> cat("Avg Train Accuracy: ", round(avg_train_acc, 4), "\n")
Avg Train Accuracy:  0.9566 

> cat("Avg Val Accuracy:   ", round(avg_val_acc, 4), "\n")
Avg Val Accuracy:    0.8486 

> cat("Overfitting Gap:    ", round(gap, 4), "\n")
Overfitting Gap:     0.108 

> cat("----------------------------------------\n")
----------------------------------------

> if(gap > 0.05) {
+   cat("WARNING: High Overfitting. Increase Dropout.\n")
+ } else if(avg_train_acc < 0.70) {
+   cat("WARNING: Underfitting. Incre ..." ... [TRUNCATED] 
WARNING: High Overfitting. Increase Dropout.
