
> # Assignment 2 MLP (Multi-Layer Perceptron) extended with class weighting
> # Supports Variable Depth (2 or 3 Layers) and Robust Scaling
> 
> librar .... [TRUNCATED] 

> library(caret)

> library(dplyr)

> library(tfruns)

> # 1. Hyperparameter flags ------------------------------------------------
> FLAGS <- flags(
+   # Regularization
+   flag_numeric("dropout1", 0),
+ .... [TRUNCATED] 

> # 2. Load & Prepare Data -------------------------------------------------
> # Ensure the path is correct
> data <- read.csv("/Users/Jujou/Documents ..." ... [TRUNCATED] 

> target_col <- "target_class" 

> x_all <- data %>% select(-all_of(target_col)) %>% as.matrix()

> y_all <- to_categorical(data[[target_col]], num_classes = 8)

> # 3. Split Data ----------------------------------------------------------
> set.seed(42)

> # Holdout Test Set (15%)
> n_rows <- nrow(x_all)

> test_idx <- sample(1:n_rows, size = floor(0.15 * n_rows))

> x_test_holdout_raw <- x_all[test_idx, ]

> y_test_holdout     <- y_all[test_idx, ]

> x_cv_raw <- x_all[-test_idx, ] 

> y_cv     <- y_all[-test_idx, ]

> # 4. Cross-Validation Setup ----------------------------------------------
> k <- 5

> folds <- cut(seq(1, nrow(x_cv_raw)), breaks = k, labels = FALSE)

> # Initialize Storage
> results <- data.frame(
+   Fold = integer(),
+   Train_Loss = numeric(),
+   Val_Loss = numeric(),
+   Train_Acc = numeric(), .... [TRUNCATED] 

> cat("Starting", k, "-Fold Cross-Validation (With Class Weighting)...\n")
Starting 5 -Fold Cross-Validation (With Class Weighting)...

> # 5. Training Loop -------------------------------------------------------
> for(i in 1:k){
+   cat("\n--- Processing Fold #", i, "---\n")
+   
+    .... [TRUNCATED] 

--- Processing Fold # 1 ---
Class Weights for Fold 1:
         0          1          2          3          4          5          6          7 
 4.7651452  1.4558824  0.1621118  1.2967480 12.1395349 43.5000000 80.8732393 21.8326996 
Fold 1 | Max Val Acc: 0.7604 (at Epoch 92)

--- Processing Fold # 2 ---
Fold 2 | Max Val Acc: 0.7657 (at Epoch 97)

--- Processing Fold # 3 ---
Fold 3 | Max Val Acc: 0.7614 (at Epoch 90)

--- Processing Fold # 4 ---
Fold 4 | Max Val Acc: 0.7718 (at Epoch 98)

--- Processing Fold # 5 ---
Fold 5 | Max Val Acc: 0.7623 (at Epoch 83)

> # 6. Final Report --------------------------------------------------------
> cat("\n========================================\n")

========================================

> cat("      ARCHITECTURE PERFORMANCE          \n")
      ARCHITECTURE PERFORMANCE          

> cat("========================================\n")
========================================

> avg_train_acc <- mean(results$Train_Acc)

> avg_val_acc   <- mean(results$Val_Acc)

> gap <- avg_train_acc - avg_val_acc

> cat("Avg Train Accuracy: ", round(avg_train_acc, 4), "\n")
Avg Train Accuracy:  0.8397 

> cat("Avg Val Accuracy:   ", round(avg_val_acc, 4), "\n")
Avg Val Accuracy:    0.7643 

> cat("Overfitting Gap:    ", round(gap, 4), "\n")
Overfitting Gap:     0.0754 

> cat("----------------------------------------\n")
----------------------------------------

> if(gap > 0.05) {
+   cat("WARNING: High Overfitting. Increase Dropout.\n")
+ } else if(avg_train_acc < 0.70) {
+   cat("WARNING: Underfitting. Incre ..." ... [TRUNCATED] 
WARNING: High Overfitting. Increase Dropout.
